name: hegema_rescue_collector
description: "HEGEMA Disaster Rescue Field Data Collector & Telemetry App (Flutter)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sensors_plus: ^5.0.0
  permission_handler: ^11.0.0
  http: ^1.1.0
  path_provider: ^2.1.0
  archive: ^3.3.0
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
